# Design Assets

- `/icons` for source iconography (Sketch/Figma exports pending).
- `/share-cards` includes templates for viral exports.
- `/tokens.json` will keep color + spacing primitives.

These are placeholders until design team commits source files.
